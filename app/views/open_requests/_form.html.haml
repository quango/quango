

-form_for [@item, @open_request], :html => {:class => "open_form form  #{local_assigns[:hide] ? "hide": ""}", :id => local_assigns[:form_id]} do |form|
  .group
    = form.label :comment, t(:comment, :scope=>"models"), :class => "label"
    = form.text_field :comment, :class => "text_field"

  .group.navform
    -if @open_request.new?
      = submit_tag t(".request_opening_details"), :class => "button", :id => "open_item_submit_#{@item.id}"
    -else
      = form.submit t("scaffold.update"), :id => "open_item_submit_#{@item.id}"
    = t("global.or")
    = link_to t("scaffold.cancel"), item_path(@item), :class => "button cancel"
