- content_for :subtabs do
  .navtitle
    = t(".title", :model => I18n.t("activerecord.models.item"))

= render :partial => "open_requests/form", :locals => {:open_request => @open_request, :item => @item }
